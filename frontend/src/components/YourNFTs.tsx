import React from 'react';
import Data from './data';

interface Props {}

function YourNFTs(props: Props) {
    const {} = props

    let imgNummer : string = data.collection.img;

    let imgUrl = 'https://ipfs.io/ipfs/ ' + imgNummer
    data.collection.img;
    return (
        <></>
    )
}

export default YourNFTs
